name: Daily Fetch Downloads

on:
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight UTC
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  fetch-downloads:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18' # Use the appropriate Node.js version

      - name: Install axios
        run: npm install axios

      - name: Run fetch-downloads script
        env:
          PACKAGE_URL: ${{ vars.NPMJS_PACKAGE_URL }}
        run: |
          echo "const axios = require('axios');
          let a = 0;
          let counter = 0;
          const maxRuns = Math.floor(Math.random() * (60 - 40 + 1)) + 40;
          const uri = process.env.PACKAGE_URL;
         

          async function addDL() {
            console.log(uri);
          }

          addDL();" > script.js
          node script.js
